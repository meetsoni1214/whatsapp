{
  "name": "event-chat",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "build": "pnpm --recursive build",
    "dev": "pnpm --parallel --filter @event-chat/api --filter @event-chat/web dev",
    "lint": "pnpm --recursive lint",
    "test": "pnpm --recursive --if-present test"
  }
}
